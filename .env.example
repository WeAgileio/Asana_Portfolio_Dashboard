# 選用：請款自訂欄位（單一變數）。逗號分隔；純數字為欄位 GID，其餘為欄位名稱。GID 優先比對。
# 未設定時會依名稱嘗試：請款金額、請款額。
# VITE_BILLING_FIELD=請款金額

# 選用：「請款進展」是／否自訂欄位（語法同 VITE_BILLING_FIELD）。未設定時依名稱「請款進展」「請款任務」。
# VITE_BILLING_TASK_FIELD=請款進展

# 選用：展示模式（建置期寫入前端 bundle）。設為 true 時建置獨立 demo 映像，使用虛構資料、略過 PAT 登入。
# VITE_DEMO_MODE=true

# 選用：後端 GET /api/* 代理快取（記憶體，毫秒）。可分級覆寫；未設定時預設各級 5 分鐘。
# ASANA_PROXY_CACHE_LIST_MS=300000
# ASANA_PROXY_CACHE_PROJECT_MS=300000
# ASANA_PROXY_CACHE_TASK_MS=300000
# ASANA_PROXY_CACHE_DEFAULT_MS=300000
# ASANA_PROXY_CACHE_MAX_ENTRIES=10000

# 選用：正式 compose 要拉取的映像（覆寫 docker-compose.prod.yml 預設）
# DASHBOARD_IMAGE=你的DockerHub使用者名/倉庫名:標籤
